<!DOCTYPE html>
<html>
  <% const {title, prompt} = config.moveFile %>
  <%- include('../partials/head', {title: title(locals.prettyName)}) %>
  <body>
    <%- include('../partials/header', {parentLinks: [], title: title(locals.prettyName) }) %>

    <div class="g-upper-body">
    <%- include('../partials/nav') %>
    </div>
    <div class="g-body">
      <div class="g-main-content" id="move-file-page">
        <h2><%- prompt(locals.prettyName) %></h2>
        <%- include('../partials/folderList', {folders, id, parents}) %>
      </div>
      <%- include('../partials/footer', { pageType: 'search' }) %>
    </div>
  </body>
</html>
