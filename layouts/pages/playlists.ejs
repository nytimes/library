<!DOCTYPE html>
<html>
<%- include('../partials/head', {title}) %>
  <body>
    <h1 class="visually-hidden">Default template</h1>
    <%- include('../partials/header', {title}) %>
    <%- include ../partials/nav %>
    <div class="g-body" id="playlist-page">
      <div class="g-left-panel">
        <%- include('../partials/playlistList') %>
      </div>
      <div class="g-main-content">
        <% if (locals.content) { %>
        <%- content %>
        <% } %>
      </div>
      <div>
      	<a id="previous" href="<%= previous %>"><%- template('playlist.nav.previous') %></a> 
      	<a id="next" href="<%= next %>"><%- template('playlist.nav.next') %></a>
      </div>
      <%- include('../partials/footer', { pageType: 'playlist' }) %>
    </div>
  </body>
</html>
